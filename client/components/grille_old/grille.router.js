/*'use strict';

angular.module('libreApp.grille')
  .config(function($stateProvider) {
    $stateProvider
      .state('grille', {
        url: '/grille',
        template: '<grille></grille>'
      })
  });*/
